[project]
name = "to-do-mcp"
version = "0.1.0"
description = "A Python-based to-do tool with MCP integration"
dependencies = [
    "mcp",
    "rich",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
